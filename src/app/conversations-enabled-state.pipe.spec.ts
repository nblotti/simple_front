import { ConversationsEnabledStatePipe } from './conversations-enabled-state.pipe';

describe('ConversationsEnabledStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConversationsEnabledStatePipe();
    expect(pipe).toBeTruthy();
  });
});
